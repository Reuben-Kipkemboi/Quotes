import { AdditionDatePipe } from './addition-date.pipe';

describe('AdditionDatePipe', () => {
  it('create an instance', () => {
    const pipe = new AdditionDatePipe();
    expect(pipe).toBeTruthy();
  });
});
